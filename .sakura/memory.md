# 项目记忆

累计反思 299 次

## 常见代码问题和审查要点

- **JSON解析安全规范**：明确解析不完整JSON时的容错策略，标major
- **公共函数调用点全量检查**：增量审查中必须列出并验证所有调用点，避免功能碎片化，标major
- **异步路由中的同步I/O阻塞**：必须使用 `asyncio.to_thread()` 包装，标major
- **i18n 键完整性**：新增 UI 元素必须同步更新翻译文件，缺失则阻断合并，标major
- **路径规范化规则细化**：禁止使用`lstrip/rstrip`移除前缀/后缀，必须使用`removeprefix/removesuffix`，对切片操作（如`domain[len(prefix):]`）需审查避免误判，标major
- **公共工具函数必须处理None/空值边界**：必须增加防御性检查，标major
- **session异常后状态污染**：捕获DB异常后必须显式rollback()或close()，标major
- **明文敏感字段存储必须加密**：新增DB列存储api_key/secret/token时必须使用加密，标阻断
- **新增状态枚举的全链路审查**：必须检查DB ENUM/迁移、历史数据回填、API返回、WebUI模板/i18n/过滤器等，标major
- **延迟导入必须注释原因**：函数体内import若无注释说明循环引用原因，阻断合并，标major
- **配置变更的"迁移完整性检查表"**：验证所有读取/写入位置是否对齐，标major
- **动态配置降级处理**：新增配置读取必须检查None/非法值场景，提供降级逻辑，标major
- **参数边界值测试**：新增数值参数必须检查负数、零值、极大值场景，标major
- **异常隔离原则**：独立功能检查应使用独立try/except，标major
- **配置清洗逻辑统一**：配置清洗逻辑应统一为工具函数，并在保存和读取时强制调用，避免分散实现，标major
- **重复逻辑检测**：跨文件重复逻辑（>10行）必须抽取公共函数，并在审查中强制检查所有调用点，标major
- **配置弃用迁移检查表**：强制要求PR输出检查表，验证配置字段在代码、WebUI翻译、文档、路由中的全链路清理，标major
- **全局状态并发安全规则**：标记模块级单例变量为major风险，要求评估热更新场景下的重置逻辑，标major
- **数据库迁移完整性检查表**：强制PR输出迁移脚本覆盖范围，包括已有部署的ALTER TABLE语句，对标critical风险
- **大文件拆分规范**：单文件超过1000行时强制建议拆分，提升可维护性，标major
- **测试数据完整性检查**：增量审查中，若生产代码新增字段提取逻辑，必须验证测试payload是否同步补全对应字段，标major
- **历史修复验证规则**：对于标记为"已修复"的问题，必须在后续审查中通过测试用例或代码检查确认修复有效性，标major
- **纯注释修改的关联文档检查**：对于纯注释修改的PR，强制检查是否关联了相关安全文档或架构决策记录，标minor
- **权限跳过逻辑的强制注释规范**：要求所有权限跳过场景必须在注释中明确说明原因和安全影响，标major
- **GitHub webhook事件字段的语义一致性检查**：验证事件字段在不同事件类型中的使用是否一致，标major
- **Webhook事件字段校验**：针对GitHub Webhook事件，强制检查事件类型与关键字段的匹配性，标major
- **API方法调用规范**：当使用PyGithub等库时，审查需验证方法名与参数类型，标major
- **关联文件同步审查**：增量审查必须检查翻译文件、模板、文档的同步更新，避免"死代码"暴露，标major
- **Shell命令标志检测规范**：要求同时检测长短标志（如`-r`/`--recursive`）和组合变体（如`-rf`），并在注释中说明设计取舍，标major
- **工具安全边界规则**：新增工具必须检查命令链绕过风险，标记为major
- **SSE异常处理规范**：所有SSE发布必须记录日志，标记为major
- **异常日志级别统一规范**：明确 `except Exception` 后的日志级别标准（如预期缺失用 `info`，真实异常用 `warning` 或 `error`），并强制审查，标major
- **异步路由阻塞检查**：新增规则要求审查异步路由中的同步 I/O 调用，必须使用 `asyncio.to_thread` 包装，标major
- **配置清洗完整性检查**：配置变更 PR 必须验证所有读取/写入位置是否对齐清洗逻辑，标major
- **工具函数边界测试**：公共函数必须补充单元测试，覆盖 `None`、空字符串、协议前缀、尾部斜杠等边界情况，标major
- **测试函数命名规范**：强制检查测试函数命名规范（pytest发现要求），必须以`test_`前缀命名，标major
- **增量审查强制检查测试文件**：避免功能重构后测试覆盖失效，必须验证测试用例与新增逻辑的匹配度，标major
- **重复辅助函数提取**：测试文件中重复定义相同辅助函数时，强制提取为模块级函数，并验证所有调用点，标major
- **常量派生函数强制测试**：任何从源数据派生常量的私有函数，必须附带单元测试，覆盖正常/异常输入，标major
- **阻断性风险明确标识**：若代码错误可导致服务启动失败（如配置解析异常），应强制标记为阻断（critical或major），而非仅建议
- **增量审查调用方验证**：对于新增/修改的工具函数，强制检查所有调用点（即使在同一文件内），避免功能碎片化
- **常量派生关系校验**：对于标记为"Derived"的常量对，强制要求添加启动时断言或自动化校验脚本，确保同步更新
- **注释语言统一规范**：项目级共享常量文件应明确注释语言规范（中文或英文），并在团队内达成一致，避免混合风格
- **模板依赖完整性检查**：对于模板文件变更，强制检查所有使用的JS函数/变量是否通过include或继承引入，避免运行时ReferenceError，标major
- **常量共享规范**：跨文件重复的安全敏感常量必须提取到共享模块，并在审查中验证引用一致性，标major
- **测试文件全链路验证**：增量审查中，新增/修改的测试文件必须验证测试用例与生产代码变更的匹配度，包括边界条件和异常场景，标major
- **配额消费顺序规范**：强制要求"验证→扣费"顺序，对任何资源消费操作标记为major风险
- **表单路由一致性检查**：前端模板新增表单时，必须同步检查后端路由是否存在，标major
- **统计查询数据隔离**：所有统计类查询必须验证owner过滤，防止数据泄露，标major

## 架构与模式审查要点

- **JSON解析场景需特别关注**：转义顺序、边界条件，建议引入单元测试
- **函数提取重构后必须检查**：是否所有调用点均使用新函数
- **全量重审价值**：功能重构后强制执行全量审查，识别系统性问题
- **类型安全最佳实践**：使用`assert`进行类型收窄和防御性编程
- **高频问题文件定期审查**：对高频问题文件定期执行全量审查
- **配置体系扩展应优先复用动态配置体系**：避免硬编码，标major
- **配置迁移PR必须输出"迁移完整性检查表"**：验证所有读取/写入位置是否对齐，标minor
- **动态配置读取调用方须处理None/非法值**：必须有降级或显式报错，标major
- **配置降级检查**：新增配置读取必须包含None/非法值的降级逻辑，标major
- **工具插件化安全边界**：工具注册机制良好，但安全边界检查存在漏洞，需重点审查
- **延迟导入的并发安全性**：项目使用异步路由，需评估延迟导入在多线程/协程下的竞态条件
- **路由层委托函数审查**：路由层出现纯委托包装函数时，需评估是否必要，优先建议直接导入使用
- **事件驱动架构抽象**：考虑引入事件处理器模式，降低webhook处理逻辑的耦合度
- **权限检查碎片化**：布尔标志跳过权限易导致逻辑分散，建议统一权限检查入口点
- **测试代码模块化**：测试文件应高度模块化，依赖`monkeypatch`进行模拟，强调测试隔离和可读性
- **测试规范执行严格性**：历史中出现过测试函数命名错误导致跳过测试的严重问题，暴露测试规范执行不严
- **常量集中管理后的全链路引用检查**：确保新增常量在所有使用点已正确替换硬编码值
- **constants.py变更影响检查表**：每次修改需列出所有依赖该常量的模块，确保全链路兼容
- **异步路由的"伪异步"审查**：无I/O的async函数需结合项目风格统一，避免不必要的协程开销
- **模板与脚本隔离风险**：WebUI模板中内联脚本易导致依赖遗漏，审查时应优先检查include语句和基模板继承
- **硬编码敏感值风险**：安全相关常量必须避免分散硬编码，优先使用配置或环境变量

## 配置管理审查要点

- **配置体系扩展应优先复用动态配置体系**：避免硬编码，标major
- **配置迁移PR必须输出"迁移完整性检查表"**：验证所有读取/写入位置是否对齐，标minor
- **动态配置读取调用方须处理None/非法值**：必须有降级或显式报错，标major
- **配置降级检查**：新增配置读取必须包含None/非法值的降级逻辑，标major

## 审查流程与质量控制

- **增量审查必须检查关联文件**：强制检查相关翻译文件、路由配置和i18n键完整性
- **全量审查触发条件**：功能重构或新增模块后强制执行全量审查
- **增量审查覆盖度下降风险**：对反复修改文件强制全链路检查
- **历史问题修复验证**：必须结合测试用例验证功能完整性
- **增量审查关联文件检查**：强制检查增量变更涉及的所有文件类型（包括测试、模板、配置）
- **增量审查必须强制检查测试文件**：避免功能重构后测试覆盖失效，必须验证测试用例与新增逻辑的匹配度
- **增量审查必须强制检查测试文件的结构规范**：包括边界测试完整性、断言一致性、类型注解兼容性
- **预存问题跟踪**：对不在当前 PR 范围内的问题，应明确标记为"待修复"并记录到项目记忆
- **配置生命周期管理**：审查时需检查废弃配置是否已彻底清理，避免"死代码"误导管理员
- **安全变更重点审查**：涉及权限、命令执行、网络访问的变更需加倍谨慎
- **异常处理一致性**：新增代码必须遵循项目现有异常处理规范
- **配置迁移完整性**：配置体系变更需建立检查表验证全链路
- **跨流程数据一致性验证**：涉及预览/创建/执行的多流程变更，必须检查核心数据在各环节是否一致
- **增量审查结合历史上下文**：增量审查需结合项目记忆和历史问题，确保全覆盖，避免覆盖度下降
- **PR整体变更验证**：增量审查应验证PR描述中提及的所有变更文件，避免仅聚焦新增提交
- **测试函数命名错误风险**：开发者常犯拼写错误（如遗漏下划线），尤其在重构命名时，需双重检查命名规范
- **大型PR强制拆分审查**：大型PR（如15000+行变更）必须强制拆分审查，避免单次审查覆盖度不足
- **测试覆盖度验证**：即使PR未新增测试，也应检查现有测试是否覆盖修复场景，避免功能回归
- **安全权衡深度评估**：权限跳过逻辑需结合系统整体安全模型评估，而非仅关注单一场景
- **增量审查调用方验证强化**：对于新增/修改的工具函数，强制检查所有调用点，即使在同一文件内
- **常量变更全链路扫描**：对热点文件（如constants.py）的增量变更，强制执行关联文件快速扫描
- **测试文件与生产代码匹配度验证**：新增/修改的测试文件必须验证测试用例与生产代码变更的匹配度

## UI 与模板审查要点

- **i18n 键完整性**：新增 UI 元素必须同步更新翻译文件，标major
- **新增WebUI模块必须检查"四一致"**：侧边栏链接、表单action、JS fetch、重定向URL与路由前缀一致，标major
- **i18n 标点完整性检查**：新增 UI 文本必须检查标点符号是否已纳入翻译键，避免混合语言标点，标major
- **配置弃用迁移检查表**：强制要求 PR 输出检查表，验证配置字段在代码、WebUI 翻译、文档、路由中的全链路清理，标major

## 经验教训与最佳实践

### 延迟导入与循环依赖
- **延迟导入注释规范**：必须严格执行注释规范，避免因注释缺失导致循环依赖问题被忽略，标major
- **延迟导入的并发安全性**：项目使用异步路由，需评估延迟导入在多线程/协程下的竞态条件
- **延迟导入是临时方案**：延迟导入是解决循环依赖的临时方案，长期应通过重构消除循环依赖

### URL 与路径处理
- **URL 构造风险**：`app_domain` 在不同场景下的语义可能不同，审查时需结合上下文分析，避免统一迁移导致逻辑错误

### 配置管理
- **配置清洗抽象**：配置清洗逻辑应统一为工具函数，并在保存和读取时强制调用，避免分散实现
- **配置清洗完整性检查**：配置变更 PR 必须验证所有读取/写入位置是否对齐清洗逻辑

### 安全审查
- **Shell 工具安全**：Shell 工具是安全关键组件，审查热点集中于此，需定期添加单元测试覆盖边缘案例
- **工具安全边界**：新增工具必须检查命令链绕过风险，标记为 major

### 异常处理
- **SSE 异常静默风险**：SSE 体系异常静默是高风险模式，会导致问题难以排查，必须强制日志记录
- **异常日志级别统一规范**：明确 `except Exception` 后的日志级别标准，强制审查

### 大型PR审查
- **大型PR强制拆分**：单次审查无法充分覆盖风险，必须强制拆分审查
- **审查覆盖度保障**：结合项目记忆和历史问题，确保全覆盖，避免覆盖度下降

### Webhook处理
- **事件字段语义一致性**：验证GitHub webhook事件字段在不同事件类型中的使用一致性
- **API方法调用验证**：使用第三方库时需验证方法名与参数类型的正确性
- **权限跳过逻辑评估**：结合系统整体安全模型评估权限跳过逻辑的影响