# 项目记忆

## 常见代码问题和审查要点

- **路径规范化禁止使用lstrip/rstrip移除前缀**：移除固定前缀必须使用`removeprefix`，禁止使用`lstrip`(字符集语义)，标major
- **公共工具函数必须处理None/空值边界**：即使注释声称“调用方不会传None”，也必须增加防御性检查，标major
- **新增公共函数必须包含docstring**：说明输入输出边界行为（如None→""），标minor
- **Mermaid标签必须完整转义**：生成图表标签时必须转义特殊字符或使用Base64编码，标major
- **清理函数入参None安全性**：公共清理函数接收可能为None的DB对象时，必须检查内部是否安全处理None，标major
- **返回值语义变更须同步调用方**：函数返回值语义变更时检查调用方变量名是否保持语义一致，跨文件超过3处升级为major
- **取消路径必须抽取公共清理函数**：凡有2个及以上取消/中断检查点，必须复用同一清理函数，标major
- **新增状态枚举的全链路审查**：新增枚举值必须检查DB ENUM/迁移、历史数据回填、API返回、WebUI模板/i18n/过滤器、SSE事件发送与前端接收，标major
- **延迟导入必须注释原因**：函数体内import若无注释说明循环引用原因，阻断合并，标major
- **session异常后状态污染**：捕获DB异常后session可能inactive，必须显式rollback()或close()，禁止仅注释声称“自动恢复”，标major
- **明文敏感字段存储必须加密**：新增DB列存储api_key/secret/token时必须使用加密，标阻断
- **API返回结构必须统一**：新增端点应返回{success,data,message}结构，标major
- **惰性重置遗漏读取路径**：所有读取用户配额/限流/统计类字段的路径必须调用重置/刷新函数，标major
- **批量UPDATE返回值语义必须明确**：若返回rowcount累加值，必须在函数名或docstring中明确说明语义，标major
- **列表页禁止逐个或批量嵌入写操作**：除非有明确性能评估，标major
- **只读接口中禁止嵌入写操作**：包括批量写，例外需注释用户量上限、操作开销评估，标major
- **限流配置必须从统一配置读取**：禁止硬编码如"10/minute"，应从settings读取，标major
- **配置降级必须记录日志**：使用默认值替代用户/环境配置时必须记录warning日志，标minor
- **热路径配置查询必须评估性能影响**：在循环或高频调用的位置查询用户配置时，必须评估DB查询频率，标major
- **权限重构必须全量覆盖**：修改权限检查逻辑时，必须扫描所有API+WebUI+CLI+后台任务中的相同/相似检查点，标major
- **策略函数必须检查输入有效性**：公共权限函数必须验证role是否在有效角色集中，无效输入返回False或抛异常，标major
- **行为变更必须标注在PR描述中**：任何权限收紧/放松、返回值语义变化必须在PR描述中明确说明，否则审查可阻断，标major
- **非关键路径异常必须评估可观测性的生效条件**：使用logger.debug记录异常时，应确认项目在调试环境中已启用debug级别，否则降级为info，标minor
- **返回None表示“跳过/无数据”时，调用方须检查是否导致重复的慢路径**，标major
- **禁止在路由层直接构造Redis key**：应封装到Service/Manager层，标major
- **except Exception:pass必须替换为显式忽略并记录debug日志**，标major
- **新增配置项若涉及行为变更，必须同步消费逻辑或注释为“预留”**：避免幽灵配置，标major
- **所有JSON API端点（除公开只读）必须显式使用CSRF依赖或头校验**：标major
- **新增DB模型的datetime字段必须使用datetime.now(timezone.utc)**：禁止utcnow，标major
- **限流/异常响应中禁止硬编码用户可见字符串**：必须使用翻译键或返回结构化键供前端本地化，标major
- **新增环境变量必须明确与现有flag的语义关系**：避免两个flag通过`or`逻辑重叠，应在文档或注释中说明是独立控制还是聚合控制，标major
- **跨平台路径处理函数必须包含单元测试**：包含相对/绝对路径、Windows/Linux路径分隔符、UNC路径边界，标major
- **开发脚本的默认配置路径必须显式加入.gitignore检查**：脚本启动时检测路径是否在仓库内，若在且未被ignore则打印警告，标major
- **Redis命令版本依赖必须检测或文档化**：新增Redis命令（如GETDEL）时，必须标注最低Redis版本，或启动时检测并降级时记录warning，标major
- **Pydantic schema中禁止dict[str, Any]（除非有充分理由）**：应使用dict[str, Union[基本类型]]或嵌套模型，标minor
- **新增原子命令必须验证异步客户端兼容性**：审查时需确认async版本的客户端支持该命令，标major
- **新增联合类型时必须扫描所有序列化/反序列化边界**：确认所有路径均接受该类型子集，标major
- **独立公共函数必须具有直接单元测试，不依赖间接覆盖**：标minor
- **“仅警告不阻断”的兼容性检测，必须在PR描述中列出受影响的下游调用及fallback状态**：标major
- **整数配置必须同时校验类型+范围**：任何从用户输入读取的整数配置，必须同时进行类型转换和业务范围校验，标major
- **配置迁移PR必须输出“迁移完整性检查表”**：列出旧配置名称→新读取方式→所有读取位置是否替换→写入位置是否对齐，标minor
- **动态配置读取调用方须处理None/非法值**：`get_dynamic_config`返回None或类型不匹配时，必须有降级或显式报错，标major
- **增量审查必须检查“范围校验是否随重构保留”**：重构将内联逻辑抽取为通用函数时，须对比旧代码的所有边界条件，标major
- **UI引导提示须确认i18n键的渲染安全性**：检查翻译内容是否包含未转义的HTML/JS，标minor
- **配置迁移PR必须包含全量调用方扫描清单**：grep扫描旧配置key的所有出现位置并逐一确认已替换，标major
- **动态配置读取必须显式声明默认值或处理None**：所有`await get_dynamic_config("key")`后必须紧跟`or default_value`或断言非空，标minor
- **配置更新操作必须触发缓存失效验证**：保存动态配置时需确认调用统一缓存清除函数，标major
- **双源配置页面需增加跨页导航提示**：配置分散在多处时应在UI上相互提供链接，标suggestion
- **开发辅助脚本中的破坏性操作（如--reset）必须在执行前要求二次确认或自动备份**，标minor
- **增量审查必须额外关注“变更文件所在模块的整体行为”**：尤其当新增功能与存量选项存在交互时，标major
- **注释中关于变量状态的描述，必须与代码的实际数据流一致**，标minor
- **finally块中的清理函数必须有独立异常处理或文档说明其不抛异常**，标major
- **新增的fallback路径必须有集成测试验证（不仅限于单元测试）**，标major
- **重复逻辑跨文件检测**：增量审查必须扫描两个及以上文件中>10行相同的逻辑，强制抽取公共函数，标major
- **禁止使用locals().get()进行条件判断**：应显式初始化变量并在作用域内直接使用，标major
- **临时属性注入必须配套显式清理方法**：若通过setattr注入临时属性，则必须提供成对的清理方法，禁止在finally中依赖属性名直接清理，标major
- **静态工厂方法不得直接依赖其他类的内部属性名**：应通过目标类提供的公开构造方法，标major
- **新增配置字段时必须审查默认值是否与现有行为兼容**：默认值是否会改变现有生产行为（如True意外启动未准备好功能），标major
- **配置常量集合合并时应显式去重或注释不重复的假设**，标minor
- **重试次数配置下限至少为1，若为0必须记录warning日志并实际执行一次无重试调用**，标minor
- **所有涉及asyncio.wait_for的超时参数，在使用前必须校验>0，否则替换为默认超时并记录warning**，标major
- **凡是WebUI中保存整数配置的地方，必须调用统一的validate_and_save_config函数，禁止在路由层直接int(raw)后写入数据库**，标major
- **配置迁移PR必须对比新旧默认值**：迁移配置时，必须在新旧默认值不一致时在PR描述中标注，若值变化需评估影响，标major
- **新建正整数解析函数必须记录warning日志**：当使用默认值替代非法输入时，记录`logger.warning`及原始值，标minor
- **无上限校验的消费方防御必须验证**：若函数未提供max_value，审查必须检查调用方是否有隐式上限（如Semaphore、数据库连接池限制），标major
- **配置解析函数必须显式处理None输入**：禁止`str(None).strip()`隐式转换，应`if raw is None: raise ValueError`，标major
- **配置默认值必须唯一声明于config.py**：禁止双源默认值，标major
- **docstring中描述的“白名单/黑名单”必须与代码常量一致**：功能语义完全相反时升为major，标major
- **重构删除内联校验时，必须输出“旧约束 vs 新逻辑对比表”**，标major
- **新增异步任务提交方式时必须评估任务丢失风险**：BackgroundTasks不适合长时间任务，应说明理由，标major
- **工具循环或后台worker的取消接口必须同时实现状态变更和中断信号**，标major
- **Shell命令白名单禁止前缀匹配**：必须解析第一个token精确匹配并拒绝shell元字符，标major
- **测试中修改全局缓存必须使用fixture或finally确保清理**，仅依赖monkeypatch不足，标major
- **新增用户级配置必须同步检查SSE、WebUI、日志、异步任务是否使用该配置**，标major
- **datetime工具函数必须集中到utils/time.py或core/time_utils.py**：即使作为ORM default，也不应放在database.py中，除非数据库模块本身是唯一使用者，标major
- **冗余导入视为代码异味**：文件顶部已导入某模块，函数内再次导入且未使用别名或延迟必要性的，应要求合并或删除，标minor
- **增量审查必须强制输出“本次未修复的历史问题”的issue追踪状态**：不能只说“应创建后续issue”，而应要求PR描述或评论中包含#issue-number或TODO(issue-xxx)，标major
- **配置范围修改必须同步更新所有测试断言**：DYNAMIC_CONFIG_RANGES变更时，强制扫描所有直接引用该范围的测试文件，标major
- **配置常量变更必须触发缓存失效检查**：审查时需确认是否存在内存/Redis缓存，并验证失效逻辑，标major
- **配置范围的“四对称性检查”**：范围定义、测试断言、路由校验、写入路径四者必须一致，标major
- **测试中的范围断言必须引用生产常量，禁止硬编码数值**，连续两次则升为major
- **int/float类型校验的语义差异必须在审查中明确标注**：重构时若合并两类校验，必须输出对比表说明截断/舍入行为是否保留，标major
- **动态配置的新增key必须扫描存量配置迁移路径**：评估超出新范围的历史值如何处理（警告/拒绝/静默修正），标major
- **通用校验函数必须是唯一真实源，不允许存在“专用块前置于通用块”**，标major

## 近期审查模式总结

- **连续未修问题升温**：同一问题连续2轮从suggestion升major，连续3轮阻断合并并自动生成issue，第4轮自动生成issue追踪
- **增量审查必须输出历史未解决问题清单并升温**：标注本次是否处理，未处理须说明理由并关联issue
- **增量审查必须输出新增公共函数的调用方扫描结果**（即使仅在模块内部使用）
- **增量审查必须输出“未直接测试但依赖的调用链”**：例如检测函数未被调用处显式测试
- **审查前强制对照项目记忆前10条major规则**，逐条检查新代码是否违反
- **公共函数副作用须用⚠️标注**：修改传入列表或对象状态时必须在docstring中标注
- **返回值语义变更的强制扫描**：返回值类型/含义变化时，必须扫描所有调用方（含跨仓库）
- **调用方扫描强制执行**：修改变更文件时须扫描导入该模块的所有调用方，增量审查不得遗漏
- **异步方法必须同步提供异步测试用例**：当新增异步函数或装饰器时，必须提供异步测试，否则阻断合并
- **事务与外部写入顺序检查**：先提交DB再写外部文件是常见反模式，审查时优先检查顺序
- **全量审查价值**：对于涉及全局行为变更（配额、调度器、配置穿透）的PR，即使增量审查已完成，也建议合并前进行一次聚焦该特性的全量审查
- **PR描述必须明确列出受影响的API端点**，审查时逐一核对安全、限流、错误处理
- **增量审查必须标注“未验证但值得关注的潜在问题”**，以“⚠️观察点”形式列出，且观察点必须有后续行动（创建issue或本PR修复）
- **“分阶段实现”PR必须列出未完成项并创建后续issue**：否则视为不完整
- **前端i18n映射函数必须支持动态翻译表**：禁止硬编码if链，使用对象映射或后端返回带前缀的key，标major
- **公共JS函数必须抽取到共享模板**：两个及以上模板出现完全相同定义必须抽取include片段，标major
- **版本检测+fallback+集成测试三件套**：对于原子操作类PR（如GETDEL），必须同时检查版本检测、运行时fallback路径、低版本环境集成测试
- **增量审查必须扫描跨模块契约变更的调用方**：联合类型、返回值语义变更时，强制输出调用方扫描清单
- **重构中的“隐性删除”检查**：当代码从多行简化为单行调用时，逐行比对旧逻辑的边界条件
- **配置类PR的对称性审查**：每看到一个`get_dynamic_config(xxx)`，必须反查是否存在对应用户写入路径
- **增量审查必须要求输出“旧逻辑约束清单”与“新逻辑是否保留”的对比表**（适用于删除旧代码+新增调用的变更）
- **配置变更checklist强制执行**：审查配置相关PR时，必须逐项检查写入路径→缓存失效、字段→消费方、默认值→兼容性
- **增量审查必须检查“被删除的逻辑的合理性”**：对每一处删除行，如原代码包含边界校验、日志、异常处理，必须评估删除是否安全
- **配置迁移PR的“对称性三件套”**：写入路径、缓存失效、默认值对比——缺一不可
- **增量审查必须检查“导出符号的调用方扫描”**：删除符号时检查`__init__.py`的导出链和`__all__`
- **双路径语义一致性修复模式**：系统grep用`-F`，Python回退用`re.escape`，实现语义统一，作为范例推广
- **Shell元字符精细化模式**：从单独`$`改为`$(`, `${`, `$('`，符合“宁细勿宽”原则
- **“点状修复”必须同时修复生成断言的辅助函数**，避免未来再次漂移
- **连续3轮未修复的minor问题自动转为issue**，升温机制执行不彻底时必须强制输出issue编号
- **增量审查必须对diff中每个文件至少一句话说明变更内容**，避免漏评（包括config等看似次要文件）

## 规范建议和经验教训

- **复合返回值的风险**：字符串拼接返回值不易扩展，优先使用结构化类型（NamedTuple/dataclass）
- **状态变更全链路清单标准化**：建议创建固定检查表（DB ENUM→模型枚举→API序列化→WebUI模板→i18n→SSE事件→状态转换矩阵）
- **SQLAlchemy异步session陷阱**：捕获异常后session进入inactive，优先使用async with session.begin()模式
- **配额类问题分析模板**：定位所有写入/消耗路径+所有读取路径（是否调用重置）
- **静态分析降级方案**：对正则提取import等方案必须明确失败降级策略
- **lstrip的双重面孔**：禁止用于前缀移除，但允许用于字符集语义，审查需区分场景
- **配置服务抽象**：建议引入ConfigService类统一封装配置降级逻辑，避免重复判断
- **XSS防护扩展**：除innerHTML外，还应检查insertAdjacentHTML、document.write、eval等危险API
- **注册表模式反模式**：当注册表数据需与UI、DB、配置文件三方同步时，改为单一真实源+缓存
- **委托模式审查清单**：行为等价性、调用链循环依赖、异常传播一致性、纯函数无副作用
- **列表收集+末尾输出的日志模式**：相比循环内多次logger.debug，这是更优的模式，可在规范中推荐
- **增量代码必须优于存量代码**：即使与旧风格一致，也必须要求遵守项目记忆规范，或创建后续issue清理
- **三态返回值的UI影响必须明确**：不能在只讨论后端缓存TTL，必须同步前端展示策略
- **内存fallback的可恢复性**：模块级dict作为Redis降级时，必须注释线程安全假设、重启丢失状态的影响，标minor
- **常量定义位置与语义保持一致**：模块级常量不得定义在函数内部（如lifespan），除非有明确生命周期绑定理由，标minor
- **新增异常处理器必须记录warn级别日志**：便于统计限流触发频率和定位问题，标minor
- **多flag组合逻辑审查**：存在两个及以上flag通过`or`/`and`控制同一行为时，审查语义重叠和用户预期清晰度
- **开发辅助脚本的安全性**：注意脚本生成的配置文件是否可能被意外提交（.gitignore + 启动时检测）
- **测试的对称性**：开发模式和生产模式必须形成“正向+反向”测试对
- **int(form.get(...))陷阱**：空字符串会抛ValueError，范围校验独立于类型转换，容易在重构时被忽略
- **动态配置系统的演进策略**：推荐引入`ConfigService.get_int(key, min, max, default)`统一封装
- **配置迁移的单元测试补充**：模拟`get_dynamic_config`返回不同值，验证回退行为
- **“声明vs验证”差距**：脚本打印安全声明时须检查是否真正调用验证逻辑，否则标记minor并注明未验证
- **别名变量的寿命陷阱**：`original_messages = messages`不创建副本，后续就地修改会直接影响别名，审查时应警惕
- **工具循环中的超限恢复**：确认回退逻辑是否允许“非首次超限”也能恢复
- **注释与代码的版本漂移**：代码重构后专门检查被修改函数附近的注释是否仍然正确
- **配置系统的“对称性”三件套**：写入路径→缓存失效、配置字段→唯一消费方、默认值→现有行为兼容
- **常量外迁后的“别名寿命”检查**：旧别名保留时确认所有调用方是否可直接替换，避免形成“僵尸别名”
- **测试隔离的脆弱性**：修改全局单例的测试即使在finally恢复，也可能因异常中断污染后续测试，优先monkeypatch或fixture作用域隔离
- **配置迁移必须物理删除旧常量**：若旧常量不再被任何代码引用（除导出语句外），必须删除，避免“幽灵常量”
- **增量审查必须扫描被删除符号的导出链**：删除一个符号时，检查`__init__.py`是否仍导出、`__all__`是否包含
- **类型转换与范围校验必须原子化**：推荐统一封装，禁止路由层裸写`int(request.form.get(...))`
- **存量技术债的“升温”机制要求**：连续两轮未修复的Minor问题，自动转为Major并阻断合并
- **“观察点”必须有后续行动**：单纯列出问题而不要求创建issue，在连续多轮后仍不会自动解决，应按“分阶段实现”规则要求补issue
- **消费方隐式上限的验证**：当配置值无业务上限时，审查应主动扫读消费方代码，确认是否存在物理上限
- **配置解析函数的日志累积风险**：配置封装函数在非法输入时至少debug级别日志，降级必须有warning
- **配置重构的“三明治陷阱”**：存在专用块+通用块且重叠范围时，审查应要求注释说明专用块存在的原因
- **测试的“假阳性容忍”**：测试值与config不匹配的问题能存活多轮，说明CI中可能未运行相关测试，应建议检查CI配置
- **增量审查的“债务碎片化”**：同一技术债跨越多个PR仍未修复，应触发自动issue创建

## 需要特别关注的领域

- **取消/中断检查点的一致性**：所有取消路径的资源清理必须统一，避免遗留状态
- **asyncio并发竞态**：webhook多次触发、配额重置并发幂等性、Event注册/注销配对
- **SQLAlchemy异步session事务边界**：取消路径中检查session有效性，异常后重建；禁止在非独占session的库函数中调用rollback()
- **返回值0的二义性**：函数返回0无法区分真无操作与调用失败，建议返回tuple或自定义结果对象
- **SSE事件发送与接收**：新增状态时必须检查所有发送SSE事件的点是否覆盖该状态
- **状态机完整性**：引入新状态时必须审查所有状态转换路径、回退是否允许
- **WebUI直接读库绕过业务逻辑**：审查所有展示配额/使用量的SQL/ORM查询，判断是否绕过了重置/刷新函数
- **路由层禁止直接操作数据库**：所有写操作必须下沉到Service层，标major
- **配置动态刷新统一性**：避免手动调用invalidate_xxx_cache的零散方式，应封装为配置服务的统一方法
- **限流+超时的成对检查**：每看到一个@limiter.limit，必须同时检查该接口内部所有外部IO是否设置了显式超时
- **测试命名与内容一致性**：测试名称必须与实际测试逻辑匹配，禁止名称漂移
- **异步测试强制要求**：新增异步函数必须提供异步测试用例
- **权限策略单一真实源**：禁止新写内联角色判断，所有if user.role比较都应改为调用策略函数
- **WebUI模板强制使用策略**：模板中也应通过上下文变量或过滤器调用策略，避免硬编码角色字符串比较
- **缓存TTL必须与下游恢复时间对齐**：如unknown状态TTL应不低于integration初始化重试间隔
- **个人用户检测的脆弱依赖**：依赖GitHub html_url字符串格式时，需增加变更检测和debug日志
- **CSRF + JSON API组合**：成为固定审查项，尤其WebUI路由下敏感操作
- **原子操作审查**：GET+DELETE、GET+SET等必须优先改成GETDEL、Lua或事务
- **WebAuthn challenge的原子消费**：必须严格一次性，防止重放
- **配置与消费的对称性审查**：每看到一个新增配置字段，强制扫描其唯一读取位置
- **Redis命令兼容性**：GETDEL等低版本不支持命令，要求版本检测+运行时fallback+集成测试三件套
- **增量审查中存量技术债升温机制**：连续多轮未处理的技术债应自动生成issue
- **动态配置系统的缓存一致性**：配置更新后是否在所有路径触发了缓存失效
- **双源配置（DB+YAML）的认知负担**：提供统一的配置抽象层或UI跨页导航提示
- **紧凑模式的降级策略**：工具循环中途超限时尝试紧凑模式，审查应确认回退逻辑是否允许非首次超限也能恢复
- **双路径设计一致性**：reviewer.py和batch_processor.py中的相似逻辑应趋于一致，审查时对比两者的回退策略
- **finally中资源清理异常安全性**：清理函数不应抛异常或必须有独立异常处理
- **Pydantic Settings + DB动态配置并存**：PR描述中必须明确“哪个配置源优先”以及“是否支持运行时覆盖”
- **frozenset常量与docstring同步**：修改常量成员集合时，必须更新所有引用该常量的函数的docstring
- **配置系统的“隐性删除”风险**：当内联校验被重构为通用函数时，必须对比旧逻辑的所有边界条件是否保留
- **增量审查的“导出符号调用方扫描”**：任何删除/修改导出的PR必须输出调用方扫描结果，检查`__init__.py`的导出链
- **双源配置错误处理一致性**：对比DB动态配置和Settings配置的错误处理行为是否一致
- **配置迁移的“对称性三件套”强制执行**：写入路径、缓存失效、默认值对比——缺一不可
- **None输入的边界处理**：配置解析函数必须显式检查None，避免`str(None).strip()`
- **SQLAlchemy + async + default函数的安全性**：确认default函数是同步的且不会阻塞事件循环
- **工具description的语义漂移风险**：工具description是LLM调用时的关键依据，必须与实现严格一致
- **grep双路径的re.escape差异**：`re.escape`转义正则元字符但不转义shell元字符，对于固定字符串匹配足够安全
- **动态配置系统的“写入路径”一致性**：WebUI/CLI/API保存配置时必须调用同一套校验和缓存失效逻辑

## 仓库信息
- 仓库名: Sakura520222/Sakura-AI-Reviewer
- 累计反思次数: 207