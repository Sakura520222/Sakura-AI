# 项目记忆

## 常见代码问题和审查要点

- **路径规范化禁止使用lstrip/rstrip移除前缀**：移除固定前缀必须使用`removeprefix`，禁止使用`lstrip`(字符集语义)，标major
- **公共工具函数必须处理None/空值边界**：即使注释声称“调用方不会传None”，也必须增加防御性检查，标major
- **新增公共函数必须包含docstring**：说明输入输出边界行为（如None→""），标minor
- **Mermaid标签必须完整转义**：生成图表标签时必须转义特殊字符或使用Base64编码，标major
- **清理函数入参None安全性**：公共清理函数接收可能为None的DB对象时，必须检查内部是否安全处理None，标major
- **返回值语义变更须同步调用方**：函数返回值语义变更时检查调用方变量名是否保持语义一致，跨文件超过3处升级为major
- **取消路径必须抽取公共清理函数**：凡有2个及以上取消/中断检查点，必须复用同一清理函数，标major
- **新增状态枚举的全链路审查**：新增枚举值必须检查DB ENUM/迁移、历史数据回填、API返回、WebUI模板/i18n/过滤器、SSE事件发送与前端接收，标major
- **延迟导入必须注释原因**：函数体内import若无注释说明循环引用原因，阻断合并，标major
- **session异常后状态污染**：捕获DB异常后session可能inactive，必须显式rollback()或close()，禁止仅注释声称“自动恢复”，标major
- **明文敏感字段存储必须加密**：新增DB列存储api_key/secret/token时必须使用加密，标阻断
- **API返回结构必须统一**：新增端点应返回{success,data,message}结构，标major
- **惰性重置遗漏读取路径**：所有读取用户配额/限流/统计类字段的路径必须调用重置/刷新函数，标major
- **批量UPDATE返回值语义必须明确**：若返回rowcount累加值，必须在函数名或docstring中明确说明语义，标major
- **列表页禁止逐个或批量嵌入写操作**：除非有明确性能评估，标major
- **只读接口中禁止嵌入写操作**：包括批量写，例外需注释用户量上限、操作开销评估，标major
- **限流配置必须从统一配置读取**：禁止硬编码如"10/minute"，应从settings读取，标major
- **配置降级必须记录日志**：使用默认值替代用户/环境配置时必须记录warning日志，标minor
- **热路径配置查询必须评估性能影响**：在循环或高频调用的位置查询用户配置时，必须评估DB查询频率，标major
- **权限重构必须全量覆盖**：修改权限检查逻辑时，必须扫描所有API+WebUI+CLI+后台任务中的相同/相似检查点，标major
- **策略函数必须检查输入有效性**：公共权限函数必须验证role是否在有效角色集中，无效输入返回False或抛异常，标major
- **行为变更必须标注在PR描述中**：任何权限收紧/放松、返回值语义变化必须在PR描述中明确说明，否则审查可阻断，标major
- **非关键路径异常必须评估可观测性的生效条件**：使用logger.debug记录异常时，应确认项目在调试环境中已启用debug级别，否则降级为info，标minor
- **返回None表示“跳过/无数据”时，调用方须检查是否导致重复的慢路径**，标major
- **禁止在路由层直接构造Redis key**：应封装到Service/Manager层，标major
- **except Exception:pass必须替换为显式忽略并记录debug日志**，标major
- **新增配置项若涉及行为变更，必须同步消费逻辑或注释为“预留”**：避免幽灵配置，标major
- **所有JSON API端点（除公开只读）必须显式使用CSRF依赖或头校验**，标major
- **新增DB模型的datetime字段必须使用datetime.now(timezone.utc)**：禁止utcnow，标major
- **限流/异常响应中禁止硬编码用户可见字符串**：必须使用翻译键或返回结构化键供前端本地化，标major
- **新增环境变量必须明确与现有flag的语义关系**：避免两个flag通过`or`逻辑重叠，应在文档或注释中说明是独立控制还是聚合控制，标major
- **跨平台路径处理函数必须包含单元测试**：包含相对/绝对路径、Windows/Linux路径分隔符、UNC路径边界，标major
- **开发脚本的默认配置路径必须显式加入.gitignore检查**：脚本启动时检测路径是否在仓库内，若在且未被ignore则打印警告，标major
- **Redis命令版本依赖必须检测或文档化**：新增Redis命令（如GETDEL）时，必须标注最低Redis版本，或启动时检测并降级时记录warning，标major
- **Pydantic schema中禁止dict[str, Any]（除非有充分理由）**：应使用dict[str, Union[基本类型]]或嵌套模型，标minor
- **新增原子命令必须验证异步客户端兼容性**：审查时需确认async版本的客户端支持该命令，标major
- **新增联合类型时必须扫描所有序列化/反序列化边界**：确认所有路径均接受该类型子集，标major
- **独立公共函数必须具有直接单元测试，不依赖间接覆盖**，标minor
- **“仅警告不阻断”的兼容性检测，必须在PR描述中列出受影响的下游调用及fallback状态**，标major
- **整数配置必须同时校验类型+范围**：任何从用户输入读取的整数配置，必须同时进行类型转换和业务范围校验，标major
- **配置迁移PR必须输出“迁移完整性检查表”**：列出旧配置名称→新读取方式→所有读取位置是否替换→写入位置是否对齐，标minor
- **动态配置读取调用方须处理None/非法值**：`get_dynamic_config`返回None或类型不匹配时，必须有降级或显式报错，标major
- **增量审查必须检查“范围校验是否随重构保留”**：重构将内联逻辑抽取为通用函数时，须对比旧代码的所有边界条件，标major
- **UI引导提示须确认i18n键的渲染安全性**：检查翻译内容是否包含未转义的HTML/JS，标minor
- **配置迁移PR必须包含全量调用方扫描清单**：grep扫描旧配置key的所有出现位置并逐一确认已替换，标major
- **动态配置读取必须显式声明默认值或处理None**：所有`await get_dynamic_config("key")`后必须紧跟`or default_value`或断言非空，标minor
- **配置更新操作必须触发缓存失效验证**：保存动态配置时需确认调用统一缓存清除函数，标major
- **双源配置页面需增加跨页导航提示**：配置分散在多处时应在UI上相互提供链接，标suggestion
- **开发辅助脚本中的破坏性操作（如--reset）必须在执行前要求二次确认或自动备份**，标minor
- **增量审查必须额外关注“变更文件所在模块的整体行为”**：尤其当新增功能与存量选项存在交互时，标major
- **注释中关于变量状态的描述，必须与代码的实际数据流一致**，标minor
- **finally块中的清理函数必须有独立异常处理或文档说明其不抛异常**，标major
- **新增的fallback路径必须有集成测试验证（不仅限于单元测试）**，标major
- **重复逻辑跨文件检测**：增量审查必须扫描两个及以上文件中>10行相同的逻辑，强制抽取公共函数，标major
- **禁止使用locals().get()进行条件判断**：应显式初始化变量并在作用域内直接使用，标major
- **临时属性注入必须配套显式清理方法**：若通过setattr注入临时属性，则必须提供成对的清理方法，禁止在finally中依赖属性名直接清理，标major
- **静态工厂方法不得直接依赖其他类的内部属性名**：应通过目标类提供的公开构造方法，标major
- **新增配置字段时必须审查默认值是否与现有行为兼容**：默认值是否会改变现有生产行为（如True意外启动未准备好功能），标major
- **配置常量集合合并时应显式去重或注释不重复的假设**，标minor
- **重试次数配置下限至少为1，若为0必须记录warning日志并实际执行一次无重试调用**，标minor
- **所有涉及asyncio.wait_for的超时参数，在使用前必须校验>0，否则替换为默认超时并记录warning**，标major
- **凡是WebUI中保存整数配置的地方，必须调用统一的validate_and_save_config函数，禁止在路由层直接int(raw)后写入数据库**，标major
- **配置迁移PR必须对比新旧默认值**：迁移配置时，必须在新旧默认值不一致时在PR描述中标注，若值变化需评估影响，标major
- **新建正整数解析函数必须记录warning日志**：当使用默认值替代非法输入时，记录`logger.warning`及原始值，标minor
- **无上限校验的消费方防御必须验证**：若函数未提供max_value，审查必须检查调用方是否有隐式上限（如Semaphore、数据库连接池限制），标major
- **配置解析函数必须显式处理None输入**：禁止`str(None).strip()`隐式转换，应`if raw is None: raise ValueError`，标major
- **配置默认值必须唯一声明于config.py**：禁止双源默认值，标major
- **docstring中描述的“白名单/黑名单”必须与代码常量一致**：功能语义完全相反时升为major，标major
- **重构删除内联校验时，必须输出“旧约束 vs 新逻辑对比表”**，标major
- **新增异步任务提交方式时必须评估任务丢失风险**：BackgroundTasks不适合长时间任务，应说明理由，标major
- **工具循环或后台worker的取消接口必须同时实现状态变更和中断信号**，标major
- **Shell命令白名单禁止前缀匹配**：必须解析第一个token精确匹配并拒绝shell元字符，标major
- **测试中修改全局缓存必须使用fixture或finally确保清理**，仅依赖monkeypatch不足，标major
- **新增用户级配置必须同步检查SSE、WebUI、日志、异步任务是否使用该配置**，标major
- **datetime工具函数必须集中到utils/time.py或core/time_utils.py**：即使作为ORM default，也不应放在database.py中，除非数据库模块本身是唯一使用者，标major
- **冗余导入视为代码异味**：文件顶部已导入某模块，函数内再次导入且未使用别名或延迟必要性的，应要求合并或删除，标minor
- **增量审查必须强制输出“本次未修复的历史问题”的issue追踪状态**：不能只说“应创建后续issue”，而应要求PR描述或评论中包含#issue-number或TODO(issue-xxx)，标major
- **配置范围修改必须同步更新所有测试断言**：DYNAMIC_CONFIG_RANGES变更时，强制扫描所有直接引用该范围的测试文件，标major
- **配置常量变更必须触发缓存失效检查**：审查时需确认是否存在内存/Redis缓存，并验证失效逻辑，标major
- **配置范围的“四对称性检查”**：范围定义、测试断言、路由校验、写入路径四者必须一致，标major
- **测试中的范围断言必须引用生产常量，禁止硬编码数值**，连续两次则升为major
- **int/float类型校验的语义差异必须在审查中明确标注**：重构时若合并两类校验，必须输出对比表说明截断/舍入行为是否保留，标major
- **动态配置的新增key必须扫描存量配置迁移路径**：评估超出新范围的历史值如何处理（警告/拒绝/静默修正），标major
- **通用校验函数必须是唯一真实源，不允许存在“专用块前置于通用块”**，标major
- **事务外的外部写入必须支持补偿或异步**：先外部存储后DB commit失败时，必须有补偿回滚或记录异常供重试，标major
- **新增公共功能模块必须至少有一处调用路径（除工具类外）**：防止死代码进入主干，标major
- **WebAuthn challenge消费失败后必须失效并重新生成**：避免重放攻击或客户端重试使用过期challenge，标major
- **所有“公开登录/注册端点”的CSRF豁免必须注释安全边界**：连续2次未落实升major，标minor→major
- **双路径（API/WebUI）新增功能必须对比现有端点的限流/CSRF/错误处理**，标major
- **测试中的mock辅助类应提取为模块级fixture共享**，避免两处以上重复定义，标minor
- **Mock方法若接收但忽略参数，应添加断言验证关键参数值**，标minor
- **datetime API迁移PR必须扫描全仓库所有测试文件中的类似mock**，输出扫描清单，标major
- **禁止在测试中直接替换内置类型（如datetime）模块级属性**，除非有不可替代理由，标minor
- **工具函数位置强制检查**：datetime/config/redis操作函数必须位于项目约定公共模块，否则标记minor，标minor
- **未修复历史问题的issue强制关联**：输出“未修复的历史问题”时必须包含#issue-number或TODO(issue-xxx)，标major
- **UI变更必须检查JS事件绑定是否依赖具体DOM结构**（如outerHTML/innerHTML替换），标minor
- **模板中内联SVG装饰图标必须添加aria-hidden="true"+focusable="false"**，标suggestion→minor（连续缺失升级）
- **PR描述与变更文件必须一致**（如描述中提到配置扩展但无对应代码变更），标major
- **共用模板的UI改动必须标注影响范围**（哪些页面/路由使用该模板），标minor
- **同步函数中使用get_running_loop().create_task时，必须评估是否可能在无运行循环的线程中调用**，无法创建任务时记录warning日志，标major
- **asyncio.create_task创建的后台任务必须在服务shutdown时有机会完成**，或在文档中明确说明“允许丢失”，标major
- **内存降级结构的清理策略必须明确最大条数上限**：若cleanup_expired无法保证上限，必须实现LRU或主动删除earliest，标major
- **异步任务调用方式必须统一**：同一模块中fire-and-forget所有位置必须使用create_task，需等待则所有位置使用await，禁止混合语义，标major
- **函数副作用必须在docstring中明确**：若函数修改外部状态，必须在docstring首部用⚠️标注，标major
- **asyncio.get_event_loop()禁止使用**：一律改为asyncio.get_running_loop()或直接使用asyncio.create_task()，标major
- **路径前缀重构须全量扫描非路由文件**：除路由、模板外，必须grep扫描services/、telegram/、tasks/中的硬编码URL，标major
- **禁止使用排除法分类请求路径**：推荐使用显式路由前缀分组或中间件标记，避免新增路由时遗漏，标major
- **外部可见链接必须通过统一服务生成**：所有写入issue、邮件、Telegram、报告的URL必须调用LinkService.get_webui_url(path)，标major
- **启动阶段依赖动态配置的操作必须显式声明配置已加载的标志或断言**，标major
- **配置加载失败路径必须审查降级行为是否安全**：异常被捕获后配置值是否仍具备合理降级，标major（原minor升级）
- **配置类PR必须输出“对称性三件套”**：写入路径→缓存失效→默认值对比，缺一不可，标major
- **新增WebUI模块必须检查“四一致”**：侧边栏链接、表单action、JS fetch、重定向URL与路由前缀一致，标major
- **批量保存配置原子性**：任一配置校验失败必须整体回滚，禁止部分写入，标major
- **敏感字段禁止传递到模板上下文**：即使未使用，也禁止将未脱敏的raw_value传入render_template，标major
- **fetch响应状态检查**：所有fetch必须检查!resp.ok，否则应抛出或回退，连续出现升major，标minor→major
- **新增路由级依赖时，必须检查所有异常处理器是否依赖该state字段**，标major
- **Depends函数若内部无await，应改为同步函数**，标minor
- **公共URL生成函数应防御性处理path格式**，标minor
- **增量审查必须扫描tests/目录中的旧函数/常量引用**，标major
- **公共函数命名升级时强制补充docstring**（从_private改为public时），标minor
- **排除法路径判断连续2轮未修复自动阻断+生成issue**，标major
- **外部链接生成收敛检查**：新增或修改*_service.py中的URL字符串时，强制检查是否应使用LinkService，标major
- **配置上限必须与消费方物理上限对齐**：新增DYNAMIC_CONFIG_RANGES