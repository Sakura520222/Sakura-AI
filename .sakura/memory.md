# 项目记忆

## 审查模式与教训
- **增量审查三大陷阱**：隧道视野（只看diff忽略架构缺陷）、零评论高分（走过场）、上下文浪费（无视多轮历史问题）
- **评分决策解耦**：增量质量高≠PR可合并，存在未闭环critical时decision必须降级
- **quick策略底线**：不等于跳过思考，必须输出摘要+理由+问题追踪表
- **净减代码蜜糖**：区分减业务逻辑（好）vs减防御代码（危险）
- **LLM信任钟摆**：从过度防御到过度信任是常见模式，正确位置在中间

## 代码高危区与审查要点
- **路径安全**：必须用`pathlib.Path.resolve()`后验证前缀，`PurePosixPath`不访问文件系统无法解析符号链接，安全场景禁止使用
- **类型契约**：跨模块混用dict/Object是系统性隐患，修复时必须横向排查
- **上帝服务**：单文件超800行混合核心逻辑与模板=架构腐化前兆
- **并发与原子性**：单例、跨文件写操作是高危区，approve前必须确认状态
- **asyncio.gather**：默认任一异常取消全部，串行改并行必须审查失败模式是否退化
- **魔法数字**：同语义数值≥2次必须提取常量
- **双数据源**：主路径+回退必须标注过渡期和收敛计划
- **防御一致性**：对称结构镜像检查边界条件；大小写归一化贯穿所有使用点

## 严重程度判定
- **三问法**：major前确认有实际bug？运行时错误？安全风险？全否则降级
- **优化建议≠major**：断言建议、延迟导入属suggestion/minor
- **延迟导入先问原因**：可能为避免循环导入，未经确认不标记
- **防御代码移除**：需等价替代或显式风险声明，禁止静默通过
- **安全类禁止降级**：路径遍历、写权限强制error

## 规范演进
- **历史阻断机制**：未解决严重问题→approve权限锁定
- **阻断表强制出现**：遗留critical每次必须出现，禁止遗漏
- **评论分区**：区分[新增问题]/[历史遗留阻断]/[确认修复]
- **路径安全建议升级为AST拦截**：不依赖LLM记忆执行resolve()规范

## 遗留关注点
- `github_write_service.py`：多轮标记，合并前全量重审安全与并发
- `sakura_memory_service.py`：800+行持续膨胀，并行化失败模式+截断移除，纳入全量审查候选
- 跨服务链路：memory部分成功+write非原子=数据不一致最短路径
- 工具自身：大PR场景文件过滤逻辑Bug待排查

## 仓库信息
- 仓库名: Sakura520222/Sakura-AI-Reviewer
- 累计反思次数: 17