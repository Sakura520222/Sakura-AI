# 深度反思 — PR #196 提交 71cf26d

## 1. 审查质量评估

**覆盖度：充足但有一个遗漏。** +16/-4 的变更范围小，审查覆盖了新增方法、调用点、测试三个维度。但遗漏了一点：`_strip_json_block` 的方法名暗示"去除 JSON 块"，实际实现却是"取 JSON 块之前的内容"——这是**命名与实现语义的偏差**。如果未来有人在不同上下文（比如 JSON 块不在末尾的场景）复用此方法，会被方法名误导。审查标记了丢弃后置内容的边界行为，但没有上升到命名语义层面。

**准确度：合理，无过度标记。** 两个 suggestion 都是防御缺口，符合 quick 策略下"防御缺口建议保留"的规范。没有误判为 major，因为当前 prompt 设计确实保证了 JSON 块在末尾且前面有 Markdown。

**完整性：对修复意图理解到位。** 准确识别了职责划分的变更——summary 从 `_apply_json_result` 移到 `parse_review_result` 统一处理，这比在两个地方各自设置 summary 更合理。

## 2. 发现的模式

**防御性切片的隐性假设：** `_strip_json_block` 的实现隐含了"JSON 块在末尾"的假设，但这个假设没有在方法文档或注释中显式声明。AI 生成代码的典型特征：实现正确但缺少对前置假设的显式化。

**遗留问题标记的"确认式疲劳"：** `parse_source` except 分支已连续 4 轮标记"未修复"，emoji 去重连续 3 轮，`line_number` 类型防御连续 3 轮。表格格式正确，但实质上已退化为"打勾仪式"——确认了状态，但没有产生任何推动力。

## 3. 规范完善建议

**新增规则：遗留问题累积升级阈值。** 当前规范有"适配器热点衰减"（同文件 5 轮评分上限 7），但缺少针对**同一 suggestion 持续未修复**的升级机制。建议：
- 同一 suggestion 连续 3 轮标记未修复 → 在主要评论中明确标注 `[累积遗留-3轮]`，引起开发者注意
- 同一 suggestion 连续 5 轮 → 评分上限 -1
- 与"适配器热点衰减"的区别：后者针对文件级别补丁疲劳，前者针对具体问题的闭环失职

**新增规则：方法命名与实现语义一致性检查。** 当方法名为 `strip_X` 但实现是"取 X 之前的内容"时，应标记 suggestion。这类偏差在 AI 辅助编码中高频出现，是未来的维护陷阱。

**完善 quick 策略 suggestion 的分层：** 当前只区分"风格偏好省略"和"防御缺口保留"。建议增加第三层：**"假设依赖型防御"**——如果建议的防御措施依赖于当前设计约束的松动（如 prompt 变化），应在建议中显式标注"当前设计下非必要，但若 XXX 变化则成为 bug"。避免开发者对建议优先级产生误判。

## 4. 经验教训

**`str.find()` 返回 -1 的切片陷阱值得确认。** 审查摘要提到"边界条件（marker 不存在）处理正确"，但未展开具体实现。`find()` 返回 -1 时，`review_text[:-1 + len(MARKER)]` 的切片行为取决于 len(MARKER) 的值——如果 MARKER 长度 > 1，切片会取到错误范围。这应该在审查中显式验证并记录，而不是一笔带过"处理正确"。

**小修小补中的评分锚定风险。** 连续 4 轮 8/10，每次增量质量确实达标，但形成了评分惯性。需要自检：如果去除历史锚定，仅看本次 +16/-4 的变更质量，8 分是否仍然合理？答案是肯定的，但这个自检过程本身很重要。

## 5. 增量审查反思

**新增提交解决了之前的问题：是。** 第 3 轮识别出 summary 被覆盖的 bug，本轮正确修复，且修复方式（职责上移到调用方）比简单删一行更优。

**增量变更引入的新风险：已识别。** `_strip_json_block` 的两个边界场景（空字符串、仅 JSON 块）都被正确标记。

**覆盖度是否因增量下降：否，但遗留问题的推动力在下降。** 这是本次反思的核心矛盾——增量审查的覆盖度对小变更足够，但遗留关注点表的存在感在递减。第 1 轮首次标记 `parse_source` 时是发现问题的价值，第 4 轮再标记时信息增量接近零，但问题本身仍未闭环。规范需要一种机制，在"信息增量归零"时将信号从"审查者标记"转化为"流程阻断"，而不是继续在表格里打勾。