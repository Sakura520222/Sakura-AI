## 深度反思报告

### 1. 审查质量评估
- **覆盖度**：本次审查识别了关键测试缺陷（恶意URL构造错误），但未深入检查支付服务层重构的关联影响。例如，`toast_redirect` 提取辅助函数后，未验证所有调用点是否已迁移（项目记忆中强调“辅助函数替换完整性验证”）。
- **准确度**：严重程度标记合理（major for 测试缺陷），但未将“测试覆盖盲区”提升至“阻断级”（项目记忆中“安全校验边界测试强制要求”标阻断）。
- **完整性**：理解了重构意图（安全逻辑提取），但未充分评估历史问题（如 `paid_amount_cents` 为 None 的降级行为）与本次变更的交互风险。

### 2. 发现的模式
- **代码模式**：开发者倾向“测试迁移”而非“测试增强”，导致测试文件职责分散（如 `test_language_switch_flow.py` 移除测试但未完善新文件覆盖）。
- **架构观察**：支付系统分层清晰（网关层、服务层），但安全校验（如URL重定向）逻辑散落在 `deps.py`，缺乏集中配置常量管理（路径排除硬编码）。

### 3. 规范完善建议
- **新增规则**：
  - **测试构造强制验证**：新增测试时，必须验证测试数据是否准确触发目标逻辑分支（如协议相对URL需确保双斜杠）。
  - **安全校验边界测试阻断级**：对URL验证、金额解析等安全关键逻辑，新增边界测试用例必须标记为“阻断”，未覆盖则禁止合并。
  - **重构关联扫描清单**：提取辅助函数后，必须列出并验证所有历史调用点迁移（项目记忆已有但未严格执行）。
- **项目特有规范**：推动路径验证常量集中化（如 `//` 开头URL的排除列表），避免硬编码散落。

### 4. 经验教训
- **测试缺陷的隐蔽性**：假通过测试比代码缺陷更危险，未来审查应优先检查测试数据构造的准确性。
- **增量审查的盲区**：本次仅关注增量文件，未扫描 `backend/webui/deps.py` 的调用链（如其他路由是否依赖 `toast_redirect`）。
- **历史问题跟踪**：`paid_amount_cents` 为 None 的降级行为虽未在本轮变更中恶化，但需在后续支付服务重构中强制修复（项目记忆中持续未解决）。

### 5. 增量审查反思
- **问题解决**：本次提交未解决历史问题（如支付金额验证降级行为），仅优化文档和重构安全逻辑。
- **新风险引入**：测试缺陷可能掩盖安全逻辑退化（如未来误删 `startswith("//")` 检查但测试仍通过）。
- **覆盖度下降**：增量审查未执行关联扫描（如支付网关重构是否影响 `toast_redirect` 的依赖），建议对支付类PR强制全量重审。