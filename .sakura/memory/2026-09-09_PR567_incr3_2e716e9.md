**一、审查质量评估**

| 维度 | 评价 | 说明 |
|------|------|------|
| 覆盖度 | ★★★★★ | 本轮审查基本覆盖了所有新增/修改的文件，尤其是 **issue_image_service.py**（内存预算、JPEG draft、原位 EXIF）和 **start.sh**（标签校验）两大风险点。测试文件也全部检查，确认了正向/负向路径。唯一可能遗漏的点是对 **Docker 镜像 digest‑pin** 的完整链路验证（在沙箱运行器中仍有间接引用），建议在后续审查中加入全库搜索。 |
| 准确度 | ★★★★★ | 所有标记的风险均为 **major / error**（内存爆炸、标签错配）并得到相应的代码防护，未出现误判为 **minor** 的情况。对 JPEG draft、DecompressionBomb 预警的处理符合 Pillow 官方建议，且对 `ImageOps.exif_transpose(in_place=True)` 的使用已消除不必要的拷贝。 |
| 完整性 | ★★★★☆ | 审查者对变更意图（降低内存占用、提升部署安全）把握较好，并在评论中提出了 **负向路径**（超限图片直接丢弃）的验证。但对 **异常日志脱敏**、**错误码统一前缀** 的检查略显薄弱，未在代码中看到对应实现。若后续加入统一错误码规范，可进一步提升完整性。 |

**二、发现的模式**

| 类别 | 观察到的具体表现 |
|------|------------------|
| **代码模式** | 1. **防御式编程**：在 `issue_image_service.py` 中先做尺寸预算、再降采样；在 `start.sh` 中使用 `fail‑closed` 检查标签一致性。<br>2. **原位操作**：`ImageOps.exif_transpose(..., in_place=True)` 体现了对内存的细粒度控制。<br>3. **显式断言**：`[[ "$component" == "web" ]]` 等 Bash 断言，避免隐式依赖。 |
| **架构观察** | 1. **高度耦合的多模块依赖**：`issue_image_service.py` 被多处测试、worker、notification 关联，属于关键业务服务，任何性能回退都会波及全链路。<br>2. **镜像标签作为部署安全栅栏**：`start.sh` 通过标签校验确保 dev‑channel 只能拉取对应的 web 镜像，体现了“标签即策略”的设计思路。<br>3. **测试驱动的增量交付**：每次功能优化都同步补充专门的负向测试，用例覆盖 50 MP 级别的大图、JPEG draft、不可缩减的超限图等，形成了完整的安全/性能回归套件。 |

**三、规范完善建议**

| 范畴 | 建议 |
|------|------|
| **审查规则** | 1. **异常日志脱敏检查**：所有 `except Exception as e:` 必须经 `sanitize(e)` 或使用项目统一的 `log_error` 包装。<br>2. **统一错误码前缀**：新增 `ERROR_CODE_ISSUE_IMG_`、`ERROR_CODE_START_SH_` 等前缀，审查时检测返回值是否符合 `ERR_<MODULE>_<NUM>` 格式。<br>3. **镜像标签校验规则**：要求所有拉取镜像的脚本（不局限于 `start.sh`）必须显式校验 `org.opencontainers.image.revision` 与 `com.sakura-ai.build.channel` 的一致性。 |
| **项目特有规范** | 1. **内存预算常量统一管理**：将 `_OVERSIZED_DECODE_BUDGET_BYTES` 抽离到 `backend/constants.py`，并在 CI 中加入 “预算不超过 X%” 的监控阈值。<br>2. **Bash 脚本 Fail‑Closed**：所有 `if` 判断后必须在错误分支中 `echo >&2` 明确错误信息并 `exit 1`，审查时检查是否出现 “silent exit”。<br>3. **图片服务测试覆盖率**：强制要求 `tests/test_issue_vision_*.py` 包含 **边界值**（`MAX_IMAGE_PIXELS`、`MAX_IMAGE_PIXELS*2`）以及 **EXIF 方向组合**（8 种）测试。 |
| **CI/安全** | 1. 在 CI 中加入 **内存峰值监控**（`pytest --max-memory=300M`），确保新代码不会突破预算。<br>2. 对 `start.sh` 增加 **Docker‑in‑Docker** 环境下的镜像拉取模拟，验证标签校验在真实镜像仓库的表现。 |

**四、经验教训**

| 教训点 | 具体建议 |
|--------|----------|
| **边缘路径的遗漏** | 以前的审查往往关注正向功能实现，负向路径（如图片超限直接丢弃）容易被忽略。本次因新增测试才捕获，今后应在审查清单中加入 “异常/资源受限路径必须有明确返回或日志”。 |
| **跨语言一致性** | Bash 脚本的安全检查与 Python 服务的异常处理需要保持同等严格度。审查时要把 **Shell** 当作 **一等代码**，使用相同的 “fail‑closed”、日志脱敏、错误码统一原则。 |
| **代码热区的累积风险** | `issue_image_service.py` 连续两轮大幅改动，热区风险提升。建议对热区文件设 **变更阈值报警**（如单 PR 行数 > 100），触发额外的全链路性能回归。 |
| **测试驱动的防护** | 本次通过新增专门的负向测试捕获了潜在的内存泄漏和标签错配，验证了 **测试即审查** 的价值。后续所有高风险改动（内存、网络、权限）均应配套负向测试。 |

**五、增量审查反思**

| 问题 | 回答 |
|------|------|
| **新增提交是否解决了之前审查提出的问题？** | 是的。<br>1. `issue_image_service.py` 通过引入 `_OVERSIZED_DECODE_BUDGET_BYTES`、JPEG draft、原位 EXIF 解决了前一次审查中发现的 **内存爆炸** 与 **ReDoS** 风险。<br>2. `start.sh` 增加的标签一致性断言直接对应了上一轮审查对 **dev‑channel 镜像错配** 的担忧。 |
| **增量变更是否引入了新的风险？** | 轻微风险仍在：<br>1. **JPEG draft** 依赖 Pillow 的内部实现，若底层库升级可能导致降采样失效，建议在 CI 中固定 Pillow 版本并监控 `source.draft` 的返回值。<br>2. **Bash 断言** 使用 `[[ ... ]]`，在旧版 `sh` 环境下可能不兼容，需在文档中明确要求 `bash` 解释器。 |
| **审查覆盖度是否因只看增量而下降？** | 部分下降。审查主要聚焦在两文件的增量，未对 **全局依赖图**（如 `backend/services/notification_service.py` 对 `issue_image_service` 的调用）进行深度回顾。建议在每次增量审查结束后，执行一次 **全库搜索**（`grep -R "decode_image"`）确认没有遗漏的旧实现仍在使用旧的解码路径。 |

---

### 总结

本次审查在 **覆盖度**、**准确度** 与 **安全防护** 上表现优秀，成功捕获并闭环了内存爆炸与镜像标签错配两大风险。通过增量审查的经验可以看到，**热区文件的持续改动需要额外的全链路回归**，并且 **负向路径的测试是防止资源泄露的关键**。后续可通过完善异常脱敏、统一错误码、加强 Bash 脚本安全规范以及在 CI 中加入内存峰值监控等措施，进一步提升审查的深度与可靠性。