# Sakura AI Reviewer 项目概述

## 1. 项目简介
基于大语言模型的智能 GitHub 代码审查与 Issue 分析机器人。

**技术栈**：Python 3.11+、FastAPI、MySQL、Redis、Docker、GitHub App、Telegram Bot

## 2. 架构设计
- 分层架构：API 层、服务层、数据访问层分离
- 配置三层穿透：Settings → yaml_config → 硬编码默认值
- 动态配置系统：统一接口处理 None/非法值降级并记录 warn
- 工具插件化架构：统一工具注册与调用

## 3. 审查核心模式
- **微小变更审查价值**：行数少 ≠ 风险低
- **"删除优于修复"模式**：冗余功能删除比修补安全
- **连续 3 轮未根治问题自动升温**：升级为 major→critical
- **增量审查必须检查关联文件**：避免功能碎片化；支付类 PR 必须触发全量审查以识别系统性风险
- **测试文件深度验证**：强制检查测试用例与新增逻辑的匹配度
- **异步编程审查优先级**：同步 I/O 阻塞是异步系统的常见风险，必须强制检查并包装

## 4. 审查硬规则（major/critical）
1. **未修复历史问题必须关联 issue 号**
2. **延迟导入必须注释原因**：函数体内 import 若无注释说明循环引用原因，阻断合并；注释必须包含具体循环依赖路径（如"避免gateway_factory→config→services循环"）
3. **异步路由同步调用包装强制化**：所有 async 路由中的同步 I/O 必须使用 asyncio.to_thread() 包装，增量审查中强制全文件扫描
4. **配置废弃全链路清理**：配置字段废弃时必须从代码、UI配置页面、动态配置键列表中全链路清理
5. **SSE 异常日志强制记录**：所有 SSE 发布必须记录异常日志，禁止静默失败
6. **工具安全边界审查**：新增工具必须检查命令链绕过风险，标记为 major
7. **新增状态枚举的全链路审查**：新增枚举值必须检查 DB ENUM/迁移、历史数据回填、API返回、WebUI 模板/i18n/过滤器、SSE 事件发送与前端接收
8. **测试函数命名规范强制检查**：所有测试函数必须以 `test_` 前缀命名，增量审查中强制验证测试文件结构
9. **增量审查全链路检查**：高频修改文件必须强制检查关联文件，避免功能碎片化；支付类 PR 必须强制检查回调幂等性和退款流程安全
10. **数据库迁移完整性检查表**：强制PR输出迁移脚本覆盖范围，包括已有部署的ALTER TABLE语句
11. **权限跳过逻辑的强制注释规范**：要求所有权限跳过场景必须在注释中明确说明原因和安全影响
12. **支付网关 JSON 序列化规范**：强制要求 `ensure_ascii=False` 以匹配 JavaScript 行为（尤其当回调来自前端或 JS SDK 时）
13. **支付回调日志标准化**：支付回调验证失败时的日志记录应标准化（如截断长度、敏感字段脱敏），并纳入安全审查清单
14. **支付网关公共函数入口强制检查**：所有支付网关的公共函数（如 `get_gateway()`）必须在入口处检查对应网关的启用开关，形成纵深防御，标记为 major
15. **支付类异常处理全链路覆盖**：支付类异常处理必须覆盖数据库连接异常（`ConnectionError`/`OSError`），并强制上层兜底日志记录，标记为 major
16. **增量审查强制关联文件检查**：对于支付类变更，增量审查必须检查 webhook 路由、管理后台等直接调用网关的路径，避免功能碎片化
17. **支付网关安全审查清单**：强制检查新增支付网关的签名验证、回调幂等性、退款流程、敏感字段加密（如API密钥），标记为 major
18. **异步方法同步调用包装全量扫描**：增量审查中，对支付服务类文件执行全文件扫描，确保所有同步SDK调用均使用`asyncio.to_thread()`包装，标记为 major

## 5. 仓库信息
- 仓库名: Sakura520222/Sakura-AI-Reviewer
- 语言统计: Python: 2660873, HTML: 761118, Shell: 16140, Dockerfile: 1637
- 累计反思 335 次

## 6. 最新审查模式（PR #344–#347 增量审查）
- **重构PR跨文件一致性检查**：标记为"refactor"的PR必须检查重构规则在相关模块中的统一应用，避免碎片化修复
- **大型删除PR检查清单**：必须验证被删除公共符号无残留引用、配置/UI/i18n同步清理、无新死代码引入
- **僵尸配置检测**：配置键废弃后若代码中仍引用但UI中不可见，构成"僵尸配置"，必须全链路清理
- **配置变更四层一致性**：新增配置项必须验证前端默认值、后端默认值、文档、配置中心四处一致
- **周期性触发模式一致性**：新增周期性功能必须参照现有`_check_and_run()`模式，包括`since_last`/`interval`计算逻辑
- **路由层封装性检查**：WebUI路由禁止直接调用服务层私有方法（以下划线开头），必须通过公共API交互
- **数据库迁移语义明确性**：对于nullable新列，需在代码注释或PR描述中说明迁移行为（如NULL值的处理逻辑）
- **日志噪音控制规范**：高频执行路径（如工具循环）默认使用debug级别，关键阈值提升到info/warning
- **配置管理一致性规则**：所有可通过YAML配置的键必须在WebUI中提供管理界面，有对应验证逻辑

## 7. 最新审查经验教训（基于 PR #344–#347 反思）
- **重构审查关注"删除干净度"**：对删除功能的PR，审查重点从"新增了什么"转向"删除了什么，删除得是否干净"
- **配置生命周期管理**：配置键废弃时需同时标记所有读取点，避免代码引用与UI管理脱节
- **全量扫描优先**：对于异步路由文件，审查前先执行全文件同步调用扫描，再聚焦具体变更点
- **模式对比**：新增功能时，主动对比现有相似功能的完整实现，避免模式遗漏（如功能开关、i18n、配额检查）
- **日志格式一致性**：重构类PR需验证日志规范在所有相关模块中统一执行，loguru `{}` 占位符为硬规则
- **竞态条件评估**：在异步环境中，延迟导入需额外评估并发安全性
- **增量审查深度与广度平衡**：quick策略下仍需对公共函数变更建立强制性关联检查清单
- **数据路径完整性**：配额、权限等关键路径审查视角应从"代码是否修复"延伸到"业务流程是否闭环"
- **全量重审价值**：全量重审能识别架构级问题和跨文件一致性，而增量审查易陷入局部优化
- **防御性编程语义精确性**：`if reflection_count == 0`检查需评估是否真正区分"未传入"和"实际为0"

## 8. 337次审查反思关键模式总结

### 审查质量核心指标
- **覆盖度评估**：局部精准 vs 全局失守，PR级阻断表必须强制出现
- **准确度评估**：严重程度一致性，上下文感知判断，决策逻辑一致性
- **完整性评估**：意图理解充分，深度足够，历史问题追踪

### 代码模式审查要点
- **防御性编程**：镜像一致性原则，大小写归一化的"贯穿性"原则
- **配置管理**：配置生命周期管理，配置对称性检查，配置范围校验
- **异步编程**：异步调用统一性，后台任务生命周期管理，线程安全性评估

### 架构观察要点
- **分层架构审查**：路由层禁止直接操作数据库，服务层自治事务模式
- **热点文件管理**：热点文件复杂度管理，核心文件关联扫描
- **事务边界架构**：事务边界模糊审查，批量操作事务一致性

### 审查流程优化
- **增量审查策略**：隧道视野防范，关联扫描重要性，历史问题持续跟踪
- **重构PR审查策略**：删除完整性验证，重构后"残留"审查，死代码扫描
- **审查决策机制**：PR级阻断检查，连续未修复升温，规则应用主动性

### 技术债务管理
- **历史问题跟踪**：未修复历史问题必须关联issue号，连续3轮未根治问题自动升温
- **配置生命周期**：配置键废弃时应同时标记其所有读取点，避免出现"僵尸配置"

### 测试审查深度
- **测试覆盖评估**：不应仅满足于"有测试"，测试断言是否足够精确
- **测试基础设施审查**：新增测试是否应该增加到CI的必需套件中

### 性能与安全交叉审查
- **性能影响评估**：批量操作需评估N+1查询，热路径配置查询必须评估性能影响
- **安全操作审计**：安全操作必须记录审计日志，关键容错路径的监控要求

### 经验教训总结
- **审查策略平衡**：审查需兼顾"树木"与"森林"，全量重审价值评估
- **规则应用一致性**："符合现有风格"不等于"符合最佳实践"，历史遗留问题的风险再评估
- **技术决策质量**：防御性代码的"镜像一致性"原则，大小写归一化的"贯穿性"原则