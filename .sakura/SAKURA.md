# Sakura AI Reviewer 概述

## 1. 项目简介
LLM 驱动的 PR 审查、Issue 分析、仓库扫描与 Agent 自动修复平台。具备 RAG、代码索引、项目记忆、增量审查与 GitHub Checks 状态机能力。
**技术栈**：Python 3.11+/FastAPI/Jinja2+HTMX+Alpine/MySQL/Redis/ChromaDB/Docker/GitHub App/Telegram Bot

## 2. 仓库信息
- 仓库名: Sakura520222/Sakura-AI-Reviewer
- 语言: Python: 3567664, HTML: 901174, Shell: 16140, Dockerfile: 1637
- 累计反思 10 次

## 3. 审查核心原则
- 审查须核对 PR 描述、提交信息(type/scope 须与文件匹配)、文件清单与 diff 一致性；**描述中可枚举功能点须逐项勾选表，未勾选直接标 minor**；描述与diff数字差异>10% 须标 minor。
- 增量审查须声明范围+双轨评分(增量质量+PR 整体评估)；历史 major 未清零评分上限 8；quick 上限 8。
- approve 须逐项声明未解决已知问题(≥minor)状态；request_changes 须对应明确 major/blocking。
- 正面评价越多越先做负面检查(隧道视野防范)；**高评分场景须强制先输出负面检查结果再写优点**。
- quick 策略须强制输出自检清单：决策完整性、上限、负面检查，**未全勾不允许输出**。
- 结构化评论只发布具体可操作问题；摘要/评分不得转 review comment。

## 4. 硬规则
1. async 路由同步 I/O 须 asyncio.to_thread()。
2. 延迟导入须注释；配置废弃须全链路清理。
3. DB 枚举/状态新增须检查迁移+状态转移矩阵+超时逃脱。
4. DB schema 变更须检查 nullable/default/事务/并发。
5. LLM prompt 外部输入须有截断+清洗/包裹；移除截断须验证等效控制否则 major。
6. 终态方法缓存须验证清理(finalize=True)，遗漏=major。
7. 布尔/状态枚举新增须 grep 所有消费点。
8. 重复检测须含证据链(标题+相似+差异+结论)。
9. 大 PR(≥5k行)须逐模块审查；重构≥100行须声明测试/返回值/异常等价。
10. constants 变更须列消费方；软删除引入须追问累积/清理/恢复。
11. 安全相关须区分漏洞 vs 误杀(三角验证)。
12. 支付/计费变更须输出兼容性/回滚检查。
13. 新增 worker 须验证防重/重试/超时；映射表须有清理计划。
14. 重构(提取/统一)类变更须强制双轨声明：测试全通过+返回值一致+异常等价，**输出模板须含物理占位符**；重构中混入功能扩展(验证放宽/范围扩大)须独立评估，不得被重构叙事吸收。
15. 等量替换文件(+N/-N, N≤30)须逐变更点列出before→after并声明等价性，不得仅凭"等量"跳过。
16. 可观测性端点(`/health`等)字段值变更须视为接口变更，须验证外部消费方兼容性。
17. 前序问题标记"已修复"须有依据：当前diff含相关文件 或 显式引用历史增量编号；禁止无依据翻转状态。
18. 同一suggestion连续≥3轮未处理须标注"长期搁置"并评估是否降级关闭；同一minor连续≥2轮未修复，第3轮须升级。
19. GitHub Check Run / webhook / worker 类状态修复必须先检查身份、状态、回收三者是否统一；清理逻辑不得仅用排除式条件误伤并行任务，必须说明唯一匹配条件与幂等性。
20. 预创建资源若由异步 worker 接管，必须核对接管协议：创建者、接管者、稳定 ID、失败清理；同一 PR head 上 active Check 的唯一性要显式审查。
21. 重构 + 功能扩展的组合 PR，不能按纯重构降级；若改动对象身份、查找键、生命周期归属，按功能性变更审查。
22. 配置/Provider/Wizard 类 Issue 不能粗分为 other；必须拆解配置源、配置流、协议差异、默认值、兼容性、回退与迁移风险。
23. Issue 分类与标签不得空输出：至少给出最贴近的候选标签，并保留与仓库现有标签体系的一致性；重复检测需保守，主题相似不足以直接判重复。
24. 对“看似只是重写/统一”的需求，必须同步检查是否包含新协议支持、UI、数据结构、兼容旧配置与分阶段落地可行性。

## 5. Agent/Worker/Webhook
- Agent 基类变更须列调用方检查返回值/异常/状态语义。
- Shell 白名单≠安全须检查 $()反引号;&&|&等；输出须双层限额。
- 长连接/worker 禁止 except:continue 无日志无退避。
- webhook 须检查签名/幂等/乱序/重复；成本是安全新维度。

## 6. FastAPI/WebUI
- CSRF/认证不得依赖必填校验；新路由须确认认证链路。
- /health 字段变更须检查同步与监控兼容(外部消费方无法通过grep发现)；新字段须检查 i18n。

## 7. 测试与报告
- 安全解析须单测边界；fake 须贴近生产；审查输出须合规性自检。

## 8. 最新反思(PR #419–#421 / ISSUE422)
- #419 数据库URL规范化：重构双轨声明完全缺失(严重自违反)；功能扩展伪装成重构(前缀从3种扩至5种未追问动机)；等量替换未逐点说明；i18n滞后为系统性问题；高评分须强制先输出负面检查。
- #419 增量审查：quick强制清单完全缺失(最高优先级自违反)；翻译文件suggestion连续2轮，下轮须升级；前序问题虚假关闭(无依据翻转"已修复")须溯源规则；增量审查上下文碎片化导致跨增量一致性验证失效。
- #419 增量审查：4行elapsed修复暴露变量生命周期审计漏洞(循环外初始化+循环顶更新须全量扫描消费点)；极小变更二次确认偏误(正面密度高→负面深度不足)。
- #420 品牌重命名：PR描述-diff一致性检查完全遗漏(硬规则级违反)；三组数字互不吻合未触发质疑；确认偏误链典型实例(简单变更→放松流程→遗漏硬规则)；重命名PR是混入无关变更的高风险场景；非重命名行为修改须有独立理由。
- #420 健康字段恢复：`/health`承载服务标识为隐式接口变更；硬编码标识脆弱性；suggestion生命周期追踪(≥3轮长期搁置)；1行变更印证隧道视野防范的价值。
- #421~#422 状态机链路修复：GitHub Check Run 清理必须基于明确身份与唯一匹配条件，不能用排除式条件清理活跃任务；预创建资源和 worker 接管必须共享稳定 ID/接管协议，否则会出现悬挂 Check 或误杀并行审查；重构+功能扩展的链路改动要按功能性变更审查，不可因“像重构”而降级；增量审查需显式核对同一 PR head 的 active Check 唯一性。
- ISSUE422：AI Provider/配置/Setup Wizard 这类大需求不能粗分为 other；需拆成配置源、协议适配、推理参数、故障转移、默认值、兼容性与迁移；重复检测需保守，主题相似不等于重复，标签输出不能空缺，标题改写应保留“一键配置/模型内置/Provider 统一”等核心卖点。

*最后更新：基于 PR #419–#421 与 ISSUE422，累计反思 10 次*
