[pytest]
filterwarnings =
    # slowapi 0.1.9 / chromadb 内部仍调用 asyncio.iscoroutinefunction，
    # Python 3.14 起弃用；等上游修复前在测试中忽略。
    # slowapi 0.1.9 / chromadb still call asyncio.iscoroutinefunction,
    # deprecated since Python 3.14; ignore until upstream fixes land.
    ignore:'asyncio\.iscoroutinefunction' is deprecated:DeprecationWarning
